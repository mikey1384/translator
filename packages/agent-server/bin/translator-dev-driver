#!/bin/sh
set -eu

launcher_dir=$(CDPATH= cd -P "$(dirname "$0")" && pwd)
repo_root=$(CDPATH= cd -P "$launcher_dir/../../.." && pwd)

exec "$launcher_dir/translator-owner-supervisor" \
  --supervise 1 -- \
  node "$repo_root/.claude/skills/run-translator/driver.mjs" "$@"
