{
  "name": "@translator/agent-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local MCP and CLI bridge for subscription-powered Translator workflows",
  "scripts": {
    "test": "node --test",
    "mcp": "node src/mcp.mjs",
    "cli": "node src/cli.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "playwright-core": "^1.55.0",
    "zod": "^4.0.0"
  }
}
