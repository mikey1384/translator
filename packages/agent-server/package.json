{
  "name": "@translator/agent-server",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "description": "Local MCP and CLI bridge for subscription-powered Translator workflows",
  "scripts": {
    "pretest": "npm run --prefix ../.. build:owner-supervisor",
    "test": "node --test",
    "premcp": "npm run --prefix ../.. build:owner-supervisor",
    "mcp": "bin/translator-dev-mcp",
    "cli": "node src/cli.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "playwright-core": "^1.55.0",
    "zod": "^4.0.0"
  }
}
